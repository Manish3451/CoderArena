services:
  - type: web
    name: codearena
    runtime: python
    pythonVersion: "3.11"
    plan: free
    buildCommand: pip install --no-cache-dir -r requirements.txt
    startCommand: uvicorn arena.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        value: "postgresql://postgres:manishpostgres@db.fdeeeajpjzmmtwsopzhy.supabase.co:5432/postgres"
      - key: ENVIRONMENT
        value: "production"
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"